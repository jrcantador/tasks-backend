pipeline {
    agent any
    stages {
        stage ('build backend') {
            steps {
                sh 'mvn clean package -DskipTests=true'
            }
        }
    }
    tools {
       maven "M2_HOME"       
    }
}