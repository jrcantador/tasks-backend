pipeline {
    agent any
    stages {
        stage ('build backend') {
            steps {
                sh 'mvn clean package -DskipTests=true'
            }
        }
        stage ('unit tests') {
            steps {
                sh 'mvn test'
            }
        }
    }
    tools {
       maven "Maveb"       
    }
}